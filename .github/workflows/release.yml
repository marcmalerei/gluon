name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: Existing lockstep version to validate without publication
        required: true
        type: string

concurrency:
  group: release-${{ github.ref }}-${{ inputs.version }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  candidate:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.61.1-noble@sha256:5b8f294aff9041b7191c34a4bab3ac270157a28774d4b0660e9743297b697e48
      options: --ipc=host --user pwuser
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0
      - run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 24.x
          cache: npm
      - run: npm ci --ignore-scripts --legacy-peer-deps
      - name: Resolve release version
        shell: bash
        env:
          DISPATCH_VERSION: ${{ inputs.version }}
        run: |
          if [[ "$GITHUB_REF_TYPE" == "tag" ]]; then
            package_version="$(node -p "require('./package.json').version")"
            recovery_path="release/recovery/${package_version}.json"
            if [[ -f "$recovery_path" ]] && [[ "$GITHUB_REF_NAME" == "$(node -p "require('./${recovery_path}').recoveryTag")" ]]; then
              echo "RELEASE_VERSION=$package_version" >> "$GITHUB_ENV"
              echo "RELEASE_TAG=$(node -p "require('./${recovery_path}').canonicalTag")" >> "$GITHUB_ENV"
            else
              echo "RELEASE_VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"
              echo "RELEASE_TAG=$GITHUB_REF_NAME" >> "$GITHUB_ENV"
            fi
          else
            echo "RELEASE_VERSION=$DISPATCH_VERSION" >> "$GITHUB_ENV"
            echo "RELEASE_TAG=v$DISPATCH_VERSION" >> "$GITHUB_ENV"
          fi
      - run: node scripts/validate-release-recovery.mjs --version "$RELEASE_VERSION" --allow-pending-evidence
      - run: npm run check:repository
      - run: npm run release:validate -- --candidate "$RELEASE_VERSION"
      - run: npm run release:artifacts -- --version "$RELEASE_VERSION"
      - run: npm run release:vscode
      - run: npm run check:release-vscode
      - name: Retain release candidate
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: gluon-${{ github.sha }}-candidate
          path: .tmp/release
          retention-days: 30
          if-no-files-found: error
          include-hidden-files: true

  publish:
    if: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v') }}
    needs: [candidate, reproducibility, create-gluon-fixtures, browser-engines, node-runtime, performance-evidence]
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      contents: write
      id-token: write
      attestations: write
    env:
      GITHUB_REPOSITORY_VISIBILITY: ${{ github.event.repository.visibility }}
      GLUON_RELEASE_ENVIRONMENT: npm
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 24.x
          package-manager-cache: false
      - run: npm install --global npm@11.18.0
      - run: npm ci --ignore-scripts --legacy-peer-deps
      - name: Resolve release version
        shell: bash
        run: |
          package_version="$(node -p "require('./package.json').version")"
          recovery_path="release/recovery/${package_version}.json"
          if [[ -f "$recovery_path" ]] && [[ "$GITHUB_REF_NAME" == "$(node -p "require('./${recovery_path}').recoveryTag")" ]]; then
            echo "RELEASE_VERSION=$package_version" >> "$GITHUB_ENV"
            echo "RELEASE_TAG=$(node -p "require('./${recovery_path}').canonicalTag")" >> "$GITHUB_ENV"
          else
            echo "RELEASE_VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"
            echo "RELEASE_TAG=$GITHUB_REF_NAME" >> "$GITHUB_ENV"
          fi
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gluon-${{ github.sha }}-candidate
          path: .tmp/release
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gluon-${{ github.sha }}-reproducibility
          path: .tmp/release
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gluon-${{ github.sha }}-performance
          path: .tmp/release/performance
      - run: node scripts/verify-release-hosting.mjs
        env:
          GH_TOKEN: ${{ github.token }}
      - run: npm run release:finalize-assets
      - name: Attest release archives and evidence
        uses: actions/attest-build-provenance@977bb373ede98d70efdf65b84cb5f73e068dcc2a # v3
        with:
          subject-path: |
            .tmp/release/packages/*.tgz
            .tmp/release/sbom/*.json
            .tmp/release/release-evidence.json
            .tmp/release/SHA256SUMS
            .tmp/release/CHANGELOG.md
            .tmp/release/RELEASE_NOTES.md
            .tmp/release/release-cut-evidence.json
            .tmp/release/compatibility-manifest.json
            .tmp/release/reproducibility.json
            .tmp/release/performance/*
            .tmp/release/vscode/*
      - name: Create immutable-release draft
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          assets=(.tmp/release/packages/*.tgz .tmp/release/sbom/*.json .tmp/release/performance/* .tmp/release/release-evidence.json .tmp/release/release-cut-evidence.json .tmp/release/compatibility-manifest.json .tmp/release/reproducibility.json .tmp/release/SHA256SUMS .tmp/release/CHANGELOG.md .tmp/release/RELEASE_NOTES.md)
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release upload "$RELEASE_TAG" "${assets[@]}" --clobber
          else
            gh release create "$RELEASE_TAG" "${assets[@]}" --verify-tag --draft --title "Gluon $RELEASE_TAG" --notes-file .tmp/release/RELEASE_NOTES.md
          fi
      - name: Publish reviewed archives directly to latest through trusted publishing
        run: npm run release:publish -- --version "$RELEASE_VERSION"
      - name: Publish VS Code extension when publisher credentials are configured
        if: ${{ secrets.VSCE_PAT != '' }}
        working-directory: editors/vscode
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: npx --no-install vsce publish --packagePath ../../.tmp/release/vscode/gluon-vscode-${{ env.RELEASE_VERSION }}.vsix --pat "$VSCE_PAT"
      - name: Verify latest registry train from a clean directory
        run: npm run release:verify-registry -- --version "$RELEASE_VERSION" --directory .tmp/release --output .tmp/release/registry-verification.json
      - run: npm run release:finalize-assets
      - name: Attest registry verification
        uses: actions/attest-build-provenance@977bb373ede98d70efdf65b84cb5f73e068dcc2a # v3
        with:
          subject-path: |
            .tmp/release/registry-verification.json
            .tmp/release/SHA256SUMS
      - name: Attach verification and publish the immutable release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "$RELEASE_TAG" .tmp/release/registry-verification.json .tmp/release/SHA256SUMS --clobber
          gh release edit "$RELEASE_TAG" --draft=false --latest

  reproducibility:
    needs: candidate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 24.x
          cache: npm
      - run: npm ci --ignore-scripts --legacy-peer-deps
      - name: Resolve release version
        shell: bash
        env:
          DISPATCH_VERSION: ${{ inputs.version }}
        run: |
          if [[ "$GITHUB_REF_TYPE" == "tag" ]]; then
            package_version="$(node -p "require('./package.json').version")"
            recovery_path="release/recovery/${package_version}.json"
            if [[ -f "$recovery_path" ]] && [[ "$GITHUB_REF_NAME" == "$(node -p "require('./${recovery_path}').recoveryTag")" ]]; then
              echo "RELEASE_VERSION=$package_version" >> "$GITHUB_ENV"
              echo "RELEASE_TAG=$(node -p "require('./${recovery_path}').canonicalTag")" >> "$GITHUB_ENV"
            else
              echo "RELEASE_VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"
              echo "RELEASE_TAG=$GITHUB_REF_NAME" >> "$GITHUB_ENV"
            fi
          else
            echo "RELEASE_VERSION=$DISPATCH_VERSION" >> "$GITHUB_ENV"
            echo "RELEASE_TAG=v$DISPATCH_VERSION" >> "$GITHUB_ENV"
          fi
      - run: npm run build
      - run: npm run release:artifacts -- --version "$RELEASE_VERSION" --output .tmp/release-reproduced
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: gluon-${{ github.sha }}-candidate
          path: .tmp/release-candidate
      - name: Compare canonical package contents
        run: npm run release:compare-artifacts -- --candidate .tmp/release-candidate --reproduced .tmp/release-reproduced > .tmp/reproducibility.json
      - name: Retain reproducibility evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: gluon-${{ github.sha }}-reproducibility
          path: .tmp/reproducibility.json
          retention-days: 30
          if-no-files-found: error
          include-hidden-files: true

  create-gluon-fixtures:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.61.1-noble@sha256:5b8f294aff9041b7191c34a4bab3ac270157a28774d4b0660e9743297b697e48
      options: --ipc=host --user pwuser
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 22.12.0
          cache: npm
      - run: npm ci --ignore-scripts --legacy-peer-deps
      - run: npm run build
      - run: npm run check:create-gluon-fixtures

  browser-engines:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.61.1-noble@sha256:5b8f294aff9041b7191c34a4bab3ac270157a28774d4b0660e9743297b697e48
      options: --ipc=host --user pwuser
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 22.12.0
          cache: npm
      - run: npm ci --ignore-scripts --legacy-peer-deps
      - run: npm run test:browser-matrix
        env:
          GLUON_BROWSER: ${{ matrix.browser }}
      - run: npm run benchmark:rendering -- --browsers=${{ matrix.browser }} --samples=10 --warmup=4 --output=.tmp/quality-evidence/rendering-${{ matrix.browser }}.json
      - run: npm run benchmark:components -- --browsers=${{ matrix.browser }} --samples=10 --warmup=4 --output=.tmp/quality-evidence/components-${{ matrix.browser }}.json
      - run: npm run benchmark:runtime -- --browsers=${{ matrix.browser }} --samples=10 --warmup=3 --output=.tmp/quality-evidence/runtime-scorecard-${{ matrix.browser }}.json
      - if: ${{ matrix.browser == 'chromium' }}
        run: npm run benchmark:bundle
      - if: ${{ matrix.browser == 'chromium' }}
        run: cp .tmp/bundle-matrix/report.json .tmp/quality-evidence/bundle-matrix.json
      - if: ${{ matrix.browser == 'chromium' }}
        run: npm run check:component-library-loader-build
      - if: ${{ matrix.browser == 'chromium' }}
        run: npm run check:storybook:component-library
      - if: ${{ matrix.browser == 'chromium' }}
        run: npm run check:shop-performance
      - name: Retain engine performance evidence
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: gluon-${{ github.sha }}-performance-${{ matrix.browser }}
          path: .tmp/quality-evidence
          retention-days: 7
          if-no-files-found: ignore
          include-hidden-files: true

  node-runtime:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [22.12.0, 24.x]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci --ignore-scripts --legacy-peer-deps
      - run: npm run build
      - run: npm run test:ssr

  performance-evidence:
    needs: browser-engines
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 22.12.0
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: gluon-${{ github.sha }}-performance-*
          path: .tmp/quality-evidence
          merge-multiple: true
      - run: npm run check:performance-evidence
      - name: Retain release performance evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: gluon-${{ github.sha }}-performance
          path: .tmp/quality-evidence
          retention-days: 30
          if-no-files-found: error
          include-hidden-files: true
