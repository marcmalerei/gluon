import { css, createComponentStyleDependency } from '@gluonjs/core';
export const avatarStyles = css`@layer atoms { :where(.gluon-avatar) { position: relative; display: inline-grid; place-items: center; overflow: hidden; inline-size: var(--gluon-avatar-size, 2.75rem); block-size: var(--gluon-avatar-size, 2.75rem); border-radius: 50%; background: var(--gluon-avatar-background, Canvas); color: var(--gluon-avatar-color, CanvasText); } :where(.gluon-avatar img) { inline-size: 100%; block-size: 100%; object-fit: cover; } :where(.gluon-avatar__fallback,.gluon-avatar__loading) { grid-area: 1/1; } :where(.gluon-avatar__loading) { inline-size: 40%; block-size: 40%; border: .15rem solid currentColor; border-inline-end-color: transparent; border-radius: 50%; animation: gluon-avatar-spin .8s linear infinite; } @keyframes gluon-avatar-spin { to { transform: rotate(1turn); } } @media (prefers-reduced-motion: reduce) { :where(.gluon-avatar__loading) { animation: none; } } @media (forced-colors: active) { :where(.gluon-avatar) { forced-color-adjust: auto; border: 1px solid CanvasText; } } }`;
export const avatarStyleDependency = createComponentStyleDependency({ id: 'gluon-atom-avatar', sheet: avatarStyles, layer: 'atom', order: 100 });
