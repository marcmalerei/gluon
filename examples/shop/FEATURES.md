# GLUON GOODS capability evidence

| Gluon capability | Shop surface | Automated evidence | Status |
| --- | --- | --- | --- |
| Optional `@gluonjs/i18n` native i18n plugin | GLUON GOODS remains single-locale in this slice because adding a customer-visible language switch requires translated product, checkout, and editorial copy as product content work; the optional package API is ready for that future route-level integration and no decorative language toggle was added | `tests/i18n.spec.ts`, `npm run typecheck:i18n`, `npm run test:i18n` | Integrated as optional i18n infrastructure; shop UI unchanged for the verified content-scope reason |
| Application runtime and plugin context | Isolated shop mount and Router plugin | `tests/shop-example.spec.ts` | Integrated |
| HTML templates and attribute spreading | Every page and reusable control | Core renderer suite + shop browser test | Integrated |
| Core `compose()` template-native functional composition | Wordmark, desktop/mobile navigation, product cards, category discovery, bag actions, and home call-to-action use typed `RouterLink` props with native HTML bodies | Core type/browser + compiler/Vite + language-server + SSR + `tests/shop-example.spec.ts` + retained DX comparison | Integrated into existing customer flows |
| Core `render()` single-node insertion, single-pass binding instantiation, cached active-document template cloning, seeded primitive text slots, conditional primitive-to-template restoration, lazy keyed anchors, bounded pristine keyed prototypes, and reduced TemplateResult/spread/unkeyed-array allocations | Dynamic product, bag, delivery, and order templates throughout the existing customer journey; the configured bag overlay closes and restores with the exact line item intact | Direct-vs-fragment, parser-reordered traversal, template owner-document/cache/instance, conditional nested-template and keyed-repeat restoration, primitive-to-structural keyed materialization, pristine cross-root clones, shared metadata, spread cleanup, array lifecycle regressions, bag reopen flow, retained rendering/allocation comparisons, and shop performance budgets | Integrated renderer optimization |
| Standalone reactivity | Bag, configuration, search, and navigation UI state | `tests/shop-example.spec.ts` + reactivity suite + retained synchronous effect benchmark | Integrated |
| Reactive platform observers: `createIntersectionObserver()`, `createResizeObserver()`, and `createMutationObserver()` | Product cards raise image fetch priority as they approach the viewport; essential image content remains native and available without observer support | Observer browser lifecycle suite + shop product discovery flow | Integrated progressive product-media loading |
| `createGluonElementRegistry()`, explicit `defineElement()`/`defineGluonElement()` registry targets, and `shadowRootRegistry` | The product configurator owns its internal add-to-bag action in an isolated registry where supported; unsupported runtimes use the documented global fallback without changing the customer flow | Scoped registry browser isolation/fallback/nested-root/reconnect suite + SSR/DSD/hydration + HMR/type contracts + Chromium/Firefox/WebKit shop flow | Integrated product-action ownership boundary |
| `@gluonjs/reactivity/signals` standard-Signal bridge | The existing abortable workshop-availability request publishes through a TC39 `State`/`Computed` graph; the product configurator consumes its connected bridge while retaining pending, error, retry, and live-status semantics | Signals Node contract + shop browser customer flow + SSR/static/hydration suites | Integrated into product availability |
| `@gluonjs/reactivity/preact-signals` optional Preact bridge | Preact is not a shop runtime dependency; the runnable `examples/signals` cart-shaped flow exercises real writable and computed Preact Signals beside the standard adapter | Real-package Node contract + runnable example production build and browser interaction | Integrated in a dedicated executable example |
| Core `createVirtualizer()` accessible list/grid virtualization | The canonical catalog has four products and fits in one viewport; virtualizing it would add scroll/focus complexity without reducing meaningful DOM work, so its complete product markup remains unchanged. The runnable `examples/virtualizer` application exercises a 500-item inventory instead | Runnable production build + browser scroll/grid/focus/resize/reorder/removal/cleanup flow + deterministic SSR and hydration fixtures + public API type/docs gates | Integrated in a dedicated executable example; shop unchanged for the verified volume reason |
| Router histories and links | Home, catalog, product deep links, active navigation | Router suite + shop browser test | Integrated |
| `@gluonjs/molecules` `NavigationStrip()` | Catalog category filters keep their native links and current-URL state while constrained layouts expose keyboard-operable previous/next controls and reveal the selected filter | Molecule overflow/resize/current-destination browser regression + `tests/shop-example.spec.ts` + desktop/390px/320px QA | Integrated into product discovery |
| `@gluonjs/atoms` `Select()` | Catalog customers sort the current product set by recency, name, or price through a native keyboard-operable select; the choice is reflected in the URL and survives route rendering | Atom semantics/exact-style/SSR/hydration tests + `tests/shop-example.spec.ts` + desktop/390px/320px QA | Integrated into product discovery |
| `@gluonjs/atoms` `Textarea()` | Optional delivery instructions use the official controlled multiline Atom while retaining native editing, selection, resize, and form submission semantics | Atom states/exact-style/SSR/hydration/visual tests + checkout customer flow in `tests/shop-example.spec.ts` + desktop/390px/320px QA | Integrated into checkout |
| `@gluonjs/atoms` `Checkbox()` | Checkout requires native shipping-and-returns consent before purchase; its checked value remains a normal submitted form value while the public indeterminate state stays presentational | Atom checked/indeterminate/form-reset/exact-style/SSR/hydration/visual tests + checkout customer flow + desktop/390px/320px QA | Integrated into checkout |
| `@gluonjs/atoms` `Radio()` | Finish, light-temperature, and cable choices use official native same-name radio groups inside their labeled fieldsets and determine the exact configured bag line | Atom grouping/Arrow-key/form/exact-style/SSR/hydration/visual tests + configured-product customer flow + desktop/390px/320px QA | Integrated into product configuration |
| `@gluonjs/atoms` `Switch()` | No current shop decision is an honest on/off preference: checkout consent is a required agreement (`Checkbox`) and product options are exclusive choices (`Radio`); adding an inert preference would manufacture product behavior | Atom Space/form/label/exact-style/SSR/hydration/RTL/forced-colors/reduced-motion and cross-engine visual tests | Shop unchanged for the verified semantic reason |
| `@gluonjs/atoms` `ToggleButton()` | Current shop buttons trigger actions, radio groups select one product option, and URL links own catalog filter state; none is a persistent independent pressed choice, so replacing them would misstate semantics | Controlled aria-pressed/preset/native-button/exact-style/SSR/hydration/icon/RTL/forced-colors and cross-engine visual tests | Shop unchanged for the verified semantic reason |
| `@gluonjs/atoms` `Progress()` | The product detail's delayed workshop-availability request shows an official indeterminate native progress control while completion is unknown, then removes it when the inventory result or error arrives | Determinate/indeterminate/exact-style/SSR/hydration/visual tests + async inventory browser flow + desktop/390px/320px QA | Integrated into product inventory feedback |
| `@gluonjs/atoms` `StatusBadge()` | Completed product availability uses the official presentational badge while the shop keeps its own in-stock/low-stock tone mapping, inventory copy, and surrounding live-status semantics | Tone/native-span/exact-style/SSR/hydration/wrapping/RTL/forced-colors visual tests + async inventory browser flow + desktop/390px/320px QA | Integrated into product inventory feedback |
| `@gluonjs/molecules` `ControlField()` | Optional checkout delivery instructions use the generic field relationship callback to connect visible label and courier help to the official Textarea without transferring value or event ownership | Input/Select/Textarea/Checkbox/Radio/Switch relationship tests + SSR/hydration/visual coverage + checkout customer flow + desktop/390px/320px QA | Integrated into checkout |
| `@gluonjs/molecules` `ChoiceGroup()` | Finish, light-temperature, and cable options use official native fieldset/legend groups around the existing caller-owned Radio values and configuration events | Radio/Checkbox/disabled/form/layout/SSR/hydration/visual tests + configured-product customer flow + desktop/390px/320px QA | Integrated into product configuration |
| `@gluonjs/molecules` `ButtonGroup()` | Search, bag, and mobile-menu actions share one named header action cluster while each caller-owned Button retains its own action semantics and source/Tab order | Group naming/source-order/attached/spaced/wrap/RTL/SSR/hydration/visual tests + shop header browser flow + desktop/390px/320px QA | Integrated into global navigation |
| `@gluonjs/molecules` `SegmentedControl()` | Catalog customers choose Grid or List presentation through one controlled pressed-button toolbar; the URL owns the view and the real product layout changes without adding tab, radio, panel, or persistence semantics | Controlled value/disabled/Arrow/Home/End/RTL/SSR/hydration/visual tests + URL-backed catalog browser flow + desktop/390px/320px QA | Integrated into product discovery |
| `@gluonjs/molecules` `Tabs()` | Product customers switch between Story and Details through stable tab/panel relationships; the URL owns selection and the shop restores focus after route navigation while panel copy remains application-owned | Automatic/manual activation, disabled, Arrow/Home/End, RTL, overflow, relationship, focus, SSR/hydration/visual tests + URL-backed product browser flow + desktop/390px/320px QA | Integrated into product information |
| `@gluonjs/molecules` `DialogSurface()` and `createDialogSurfaceController()` | The bag uses the official labelled ARIA dialog surface and end-drawer presentation; Escape and direct-overlay dismissal close caller-owned bag state while initial, contained, and return focus use the public controller | Dialog naming/description/regions, focus lifecycle, Escape/overlay, SSR/hydration/visual tests + add/quantity/empty/checkout bag flows + desktop/390px/320px QA | Integrated into the bag |
| `@gluonjs/molecules` `Disclosure()` | Shipping customers expand real tracking, packaging, and remote-area details through native details/summary behavior while the policy owns copy and state | Native open/toggle/keyboard/unavailable relationships, SSR/hydration/visual tests + shipping route flow + desktop/390px/320px QA | Integrated into shipping policy |
| `@gluonjs/molecules` `Accordion()` | Shipping customers open one tracking, packaging, or remote-area topic at a time; the URL owns the selected topic while each item retains native Disclosure semantics | Controlled single/multiple values, heading/group relationships, Arrow/Home/End/unavailable behavior, SSR/hydration/visual tests + URL-backed shipping route flow + desktop/390px/320px QA | Integrated into shipping policy |
| `@gluonjs/molecules` `InlineNotice()` | Customers receive polite, bounded order-confirmation feedback containing the real order ID, delivery email, total, and a caller-owned continue-shopping action | Tone/live/static/action/dismiss/RTL/wrapping/SSR/hydration/visual tests + checkout-to-confirmation browser flow + desktop/390px/320px QA | Integrated into order confirmation |
| Adopted stylesheets | Complete responsive shop presentation | Shop browser test + visual QA | Integrated |
| `installUi()` shared foundation/theme owner, app-owned token preset, and named SSR selection | `createShopApplication()` owns one target-scoped UI lifetime in CSR and hydration, retains the documented GLUON GOODS token preset and layout sheet through that owner, validates the shared carriers, and releases shared, exact-component, token, and app sheets on every unmount path | `tests/shop-example.spec.ts`, `tests/hydration.spec.ts`, `tests-node/ssr.spec.ts`, `tests/memory-retention.spec.ts`, production shop build | Integrated owner lifecycle |
| `Component.styles` and usage-driven exact sheet ownership | The real catalog search renders the official `Input` only while the search dialog is active; its exact sheet is adopted before display and released when the dialog closes, without category imports | `tests/shop-example.spec.ts`, `tests/component-styles.spec.ts`, `tests/hydration.spec.ts`, `tests-node/ssr.spec.ts`, `check:ui-contract` dynamic production fixtures | Integrated into search, renderer lifecycle, and universal transport |
| `GluonElement`, `@customElement`, `@property`, typed events/slots, and form-associated Custom Elements | Product configuration is the same public decorated `gluon-product-configurator` boundary in GLUON GOODS and the maintained Vue 3 host | `tests/decorators.spec.ts`, `tests/vue-migration-interop.spec.ts`, `tests/docs-examples.spec.ts`, shop customer flow, SSR/hydration regression, desktop/390px/320px QA | Integrated incremental migration boundary |
| `defineGluonElement()`, `elementProperty()`, `elementEvent()`, keyed setup state, lifecycle cleanup, and inferred exposed methods | The bag's live quantity/remove control is an app-local functional autonomous Custom Element with optimistic cancelable quantity changes, native removal events, keyboard focus, 44px actions, and reconnect/HMR ownership | `tests/functional-element.spec.ts`, `tests/shop-example.spec.ts`, `tests/vite-hmr.spec.ts`, SSR/hydration, Devtools, test-utils, memory-retention, compiler/LSP, public API type/example gates, desktop/390px/320px QA | Integrated concise stateful boundary |

The separate retained T4 comparison under `benchmarks/dx/stateful-form-control`
uses the same quantity-control contract for the prior Gluon class API, the
functional API, pinned Vue, and pinned React. Its metrics and limitations are
validated independently from the shop's customer-flow evidence.
| Native dialog and control accessibility | Search, mobile menu, product choices, and bag | `tests/shop-example.spec.ts` + 390px/320px browser QA | Integrated |
| Official Store | Per-app bag state, configured line items, persisted bag, derived totals | Store Node/type suites + `tests/shop-example.spec.ts` | Integrated |
| Store actions + Router forms | Labeled delivery checkout, order summary, atomic order placement, and confirmation URL | Desktop/mobile shop flow + Store snapshot and browser assertions | Integrated purchase path |
| `Suspense` and async component contract | Abortable product availability loading, explicit pending/error/retry states | Built-ins browser suite + `tests/shop-example.spec.ts` | Integrated |
| `Teleport` and `Transition` | Application-owned, animated accessible bag overlay | Built-ins context/cleanup suite + shop browser test | Integrated |
| `KeepAlive` | Route view retention across product back/forward traversal | Built-ins LRU suite + shop node-identity assertion | Integrated |
| `TransitionGroup` | Keyed bag line insertion/removal/movement | Built-ins identity/reduced-motion suite + shop bag flow | Integrated |
| `LayoutTransition()` render-to-render geometry | The existing catalog grid preserves spatial continuity when category URLs change the visible product set; reduced-motion users receive the immediate final grid | Built-ins FLIP/identity/cancellation/SSR regression + catalog customer flow + desktop/390px/320px evidence | Integrated into product discovery |
| `@gluonjs/test-utils` and `@gluonjs/test-utils/ssr` | Acceptance infrastructure only; it has no honest customer-facing shop surface | Package browser/type contracts plus the real product-route SSR transport, retained hydration DOM, interactive add-to-bag, exact style handoff, and complete resource cleanup; shop remains on its production app API | Integrated as browser and universal test infrastructure |
| `@gluonjs/vite` | Shop development/build pipeline; compatible page, Store, Custom Element, and adopted stylesheet edits retain live state | Compiler contracts + real Vite/Chromium HMR flow + production-bundle scan | Integrated as build infrastructure |
| `@gluonjs/ssr`, hydration, and static entry points | Isolated dynamic response plus five prerendered routes, mixed `/products/:slug` fallback, identity-preserving browser handoff, and initial styled content using the same shop modules | SSR/static fixtures + asset manifest + browser Router/Store/style ownership and add-to-bag assertions | Integrated universal rendering and deployment |
| `@gluonjs/ssr/eleventy` | Deployment infrastructure only; the same home, catalog, and product customer routes are rendered from canonical shop modules without adding a route or control | Real Eleventy 3.1.6 build + Vite-visible-content parity + assets/styles/state/CSP transport + request isolation/cleanup + browser product hydration | Integrated as an optional deployment adapter; shop UI unchanged |
| `create-gluon` complete UI projects and app-local component generation | Acceptance infrastructure only; scaffolding/generation is a developer filesystem workflow and has no honest customer-facing browse, configure, bag, or checkout control | CLI unit/type/security/language contracts + install/typecheck/template-check/browser-test/build verification for all 20 selections, computed UI styles, state-preserving token/consumer HMR, no-recovery UI hydration, and install/typecheck/template-check/browser-test/build/pack verification for all five generated kinds | Integrated as developer infrastructure; shop unchanged for the verified reason |
| `@gluonjs/language-server` | Developer tooling only; it has no honest customer-facing shop surface | Shared analyzer + protocol/type contracts + generated starter template checks | Integrated as editor and CI infrastructure |
| `analyzeStaticGluonProject()`, `PROJECT_ANALYSIS_SCHEMA`, and `gluon-project-analyze` | Static developer infrastructure has no honest customer-facing browse, configure, bag, or checkout control; the canonical shop sources are its retained input | Versioned `project-analysis.json` + deterministic/confidence unit coverage + path-safe zero-write CLI + packed clean-install fixture | Integrated as canonical project-analysis evidence; shop UI unchanged |
| `@gluonjs/devtools-api` and `@gluonjs/devtools` | Opt-in development inspection only; production GLUON GOODS does not expose the bridge | Protocol Node suite + browser multi-app/render/Router/Store/inspector suite + production-disabled Vite assertion | Integrated as development infrastructure |
| Playground and diagnostic catalog | Separate developer reproduction/reference surface; it has no honest customer-facing shop application | Catalog/source parity + compact-code fixture + Playground URL/archive/browser flow + production build | Integrated as diagnostic infrastructure |
| Cross-engine CI and performance budgets | Acceptance infrastructure only; no customer-facing control belongs in the shop | Full GLUON GOODS browser suite in Playwright Chromium, Firefox, and WebKit + production bundle and home/product/bag/checkout p95 budgets + retained workflow artifacts | Integrated as release-quality evidence |
| Production example previews and Node-safe static-build verification | Deployment infrastructure only; preview commands serve existing production outputs and add no customer-facing shop control | `check:example-previews` HTTP smoke coverage + `check:shop-static` client/SSR/static sequence, route manifest, assets, and hydration validation | Integrated as deployment evidence; shop flow unchanged |
| Versioned documentation, generated API reference, report-only Vue analyzer, and codemod no-go evidence | Developer education and migration evidence only; the 623 reviewed task-oriented API examples, Node source analysis, and zero-write decision have no honest customer-facing shop control | `npm run check:docs`, exact reviewed-example coverage for all 623 symbol pages, strict snippet compilation, documented-symbol use, generic-copy/compiler-placeholder rejection, analyzer schema/CLI/fixture gates, `npm run check:vue-codemod-decision`, compiled documentation examples, and public Pages deployment; the analyzer exercises the Vue host and production configurator sources without adding a shop control | Integrated as documentation and migration infrastructure |
| Accessibility, security, and retention gates | Existing browse, configure, bag, and checkout journey; no decorative QA surface is added | axe WCAG A/AA journey scan + focus assertions + repeated teardown retention + validated threat model | Integrated as release-quality evidence |
| `@gluonjs/quarks` `createFocusScope()` | Search, mobile-menu, and bag-dialog focus entry, containment, and return | Shop keyboard-flow assertions in every browser engine lane | Integrated into the customer journey |
| Official Atoms and typed UI extensibility (`Button`, `Input`, `defineButtonPreset`, `defineIcon`) | Header, menu, dialog, product add/retry, and bag quantity/remove actions use app-owned Button presets; catalog search uses `Input`; `ProductConfigurator` remains the stateful form-associated Custom Element and composes its action inside the existing ShadowRoot | `tests/shop-example.spec.ts`, `tests/accessibility-gates.spec.ts`, `tests/hydration.spec.ts`, `check:ui-contract`, Chromium/Firefox/WebKit customer flow | Integrated into browse, configure, and bag flows |
| `defineUiAtom()` concise presentational authoring | The desktop and mobile Journal navigation use one app-owned `ShopEditorialLink` Atom with declarative native anchor/span selection and one public props object | `tests/ui-extensibility.spec.ts`, public type/example gates, `tests/shop-example.spec.ts`, desktop/390px/320px QA | Integrated into global navigation |
| `.gluon` presentational SFC compilation | `ShopEditorialLink` keeps its typed props, conditional native anchor/span root, default slot, and owned sheet in one file; Vite lowers it to public Atom/Quark/style contracts used by both desktop and mobile Journal navigation | compiler/Vite unit and production-build coverage, `tests/shop-example.spec.ts`, static/SSR/hydration builds, desktop/390px/320px QA | Integrated into global navigation |
| `q.textarea()` raw-text-safe default and controlled-value bindings | The official `Textarea` Atom composes the native textarea Quark for optional delivery instructions without a raw-text child expression | Quark browser regression + Atom contract + checkout customer flow in `tests/shop-example.spec.ts` | Integrated into the purchase path |
| `@gluonjs/graph` interactive network canvas | A knowledge-network explorer is a developer/research surface, not a customer browse, configure, bag, or checkout task; forcing it into GLUON GOODS would manufacture a decorative dashboard | `tests/graph.spec.ts`, runnable `docs-site/examples/graph.html`, and desktop/mobile browser evidence | Integrated in a dedicated executable example; shop unchanged for the verified journey reason |
| Repeated Molecule and page Organism composition (`FormField`, `defineMolecule`, `defineOrganism`) | Five labeled required delivery fields use official `FormField`; `PurchaseAction` is the app Molecule; `CheckoutExperience` is the real one-form checkout page Organism with order summary and purchase action | Checkout form semantics/customer flow in `tests/shop-example.spec.ts`, axe gates, SSR/hydration sheet order, production marker/tree-shaking gate | Integrated into the purchase path |
| `@gluonjs/json-forms` schema-driven form Custom Element | GLUON GOODS’ checkout deliberately retains its bespoke, product-aware delivery form; replacing that customer surface with a generated schema form would be decorative and would weaken its existing flow. The maintained delivery-preferences reference is the honest schema-authored operations form. | `tests/json-forms.spec.ts`, `npm run check:docs`, and desktop/390px browser interaction of `docs-site/examples/json-forms.html` | Integrated as a documented operations-form capability; shop unchanged for the verified reason |
